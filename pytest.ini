[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -ra --strict-markers --cov=backend/services --cov-report=term-missing --cov-fail-under=85

[coverage:run]
source = backend/services

[coverage:report]
show_missing = True
fail_under = 85
